# 整理版验证记录

日期：2026-09-14。目标：将当前遥操和采集实现整理成无需相邻工程目录的独立源码仓库。

| 项目 | 结果 |
| --- | --- |
| 独立安装 | 在带空格的新目录创建 Python 3.11 遥操环境、独立 Placo 环境、Python 3.12 采集环境，安装成功 |
| 111 项遥操测试 | 在上述新环境通过，包括双肩映射、回位、夹爪、输入中断、虚拟 CAN 和遥测 |
| 17 项采集测试 | 在上述新环境通过，包括 HDF5 保存、相机/关节对齐、频率、片段筛选、两个 Parquet/四个视频和 LeRobot 加载 |
| 8 项可移植性测试 | 路径含空格、模型依赖闭合、相机分配、歧义/重复序列号、OpenPI 路径迁移、安装模式冲突、双 CAN 只读预览及打包排除规则通过 |
| 浏览器 | 合成数据录制、回放/PASS、改名、刷新保留、选择两段导出通过；1440×1000 与 390×844 无横向溢出和脚本错误 |
| XRoboToolkit 下载 | Ubuntu 22.04 包和 PICO 1.1.1 SDK29 APK 下载、SHA-256 校验和解压通过 |
| XR 版本一致性 | 下载的 PC Service 与 C SDK 和原部署逐字节一致 |
| 离线预检 | 新目录模型、pyAgxArm V120、Placo 进程/FK 一致性、C SDK 加载通过；CAN 命令数量为 0 |

此次没有发送真实机械臂运动指令，没有切换正在运行的采集服务，也没有修改原工程的录制数据。
现有机器为 Ubuntu 22.04，独立环境验证不等于在全新操作系统安装上验证；Ubuntu 24.04 下载配置已提供但未运行实机测试。
尚未执行 OpenPI GPU 训练或采集后的策略部署。本仓库不宣称更换安装、机械零位或 TCP 后可以直接沿用现场标定。

## 2026-09-15：速度与手动结束录制

| 项目 | 结果 |
| --- | --- |
| 112 项遥操测试 | 全部通过，包括双臂回位步进收敛及速度边界、60 mm/s 夹爪命令、摇杆方向与重复输入处理 |
| 22 项采集测试 | 全部通过，包括回位 state/action 对齐、停用旧时长上限、延迟反馈重试、写入队列重试、断流后同片段恢复、等待期间手动保存和缺帧质检 |
| 浏览器 | 合成数据录制、回放、质检、选择两段导出及等待数据时手动保存通过；1440×1000 与 390×844 无脚本错误和横向溢出 |
| 已有数据兼容 | 旧 manifest 中的时长上限不会阻止新版打开原目录，已有片段与相机配置保留 |
| 本机采集服务 | 在旧采集器待机且没有导出任务时切换为整合版；两路相机与遥测可用，原 6 个片段可读取 |

本次未发送真实机械臂运动指令，也未重启正在运行的遥操程序。速度验证使用虚拟 CAN 和模型步进，实际开合与回位速度需在遥操重启后实机确认。浏览器录制测试使用临时合成数据，没有向原录制目录添加测试片段。
